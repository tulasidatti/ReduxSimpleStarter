import { renderComponent, expect } from '../test_helper';
import WeatherList form '../../src/containers/weather_list'

describe('WeatherList', ()=> {
	let component;

	beforeEach(()=> {
		component = renderComponent(WeatherList);
	});

	it('shows tr to represent weather for each city', () {

	});
})